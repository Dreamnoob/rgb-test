# Start Theme — WordPress Theme

## 📋 Опис проекту

WordPress тема з кастомними секціями та функціоналом для відображення нерухомості.

---

## ✅ Реалізований функціонал

### 🔸 Секція "Properties" (Custom Post Type + AJAX-сортування)

#### Що було реалізовано:

1. **Custom Post Type `property`**
   - Реєстрація через `inc/property-post-type.php`
   - Підтримка стандартних полів WordPress (title, editor, thumbnail, excerpt)
   - Публічний тип запису з архівом

2. **ACF поля для Property**
   - **Ціна** (`price`) — обов'язкове числове поле
   - **Ціна за м²** (`price_per_m2`) — текст
   - **Адреса** (`address`) — обов'язкове текстове поле
   - **Кількість спалень** (`bedrooms`) — текст
   - **Кількість ванних кімнат** (`bathrooms`) — текст
   - **Кількість поверхів** (`floors`) — текст
   - **Короткий опис** (`description`) — textarea

3. **AJAX-сортування за ціною**
   - Кнопка сортування з перемиканням ASC/DESC
   - Візуальна індикація напрямку сортування (стрілка та підпис)
   - Асинхронне оновлення списку без перезавантаження сторінки
   - Збереження кількості відображених елементів при сортуванні (якщо завантажено 9, після сортування залишається 9)

4. **AJAX-підвантаження (Load More)**
   - Кнопка "Load More" для асинхронного завантаження додаткових записів
   - Автоматичне приховування кнопки, коли всі записи завантажені
   - Показ загальної кількості записів (`X items`)

5. **Налаштування відображення**
   - Початкова кількість елементів: **3**
   - Кількість елементів на сторінку: **3**
   - Відображення загальної кількості записів

#### Технічні деталі:

**Файли:**
- `inc/property-post-type.php` — реєстрація CPT та AJAX-обробники
- `template-parts/sections/properties.php` — шаблон секції
- `template-parts/property-card.php` — шаблон картки property
- `scripts/components/properties.js` — JavaScript для сортування та підвантаження
- `scss/sections/_properties.scss` — стилі секції
- `acf-json/group_property.json` — ACF поля для синхронізації

**AJAX-обробка:**
- Action: `load_properties`
- Nonce: `properties_nonce`
- Параметри: `sort_order`, `page`, `per_page`

**Особливості реалізації:**
- При сортуванні зберігається кількість вже завантажених елементів
- Відсутність автоматичного скролу при сортуванні (користувач залишається на місці)
- Правильна робота пагінації після сортування
- Локалізація AJAX-параметрів через `wp_localize_script` в `functions.php`

---

## 🛠 Технології

- **WordPress** (нативний функціонал)
- **ACF (Advanced Custom Fields)** — для кастомних полів
- **jQuery** — для AJAX-запитів та DOM-маніпуляцій
- **SCSS** — для стилізації
- **Gulp** — для збірки асетів

---

## 📁 Структура файлів

```
start-theme/
├── inc/
│   └── property-post-type.php      # Реєстрація CPT та AJAX
├── template-parts/
│   ├── sections/
│   │   └── properties.php         # Шаблон секції
│   └── property-card.php          # Шаблон картки
├── scripts/
│   └── components/
│       └── properties.js          # JS для сортування та Load More
├── scss/
│   └── sections/
│       └── _properties.scss       # Стилі секції
└── acf-json/
    └── group_property.json         # ACF поля
```

---

## 🚀 Використання

### Створення нового Property:

1. Перейдіть в адмін-панель WordPress
2. Відкрийте **Properties → Add New**
3. Заповніть обов'язкові поля (Ціна, Адреса)
4. Додайте опціональні дані (спальні, ванні, поверхи, опис)
5. Встановіть зображення запису (Featured Image)
6. Опублікуйте запис

### Використання секції Properties:

Секція автоматично виводиться через шаблон `template-parts/sections/properties.php`.

Можна використовувати через:
- `get_template_part('template-parts/sections/properties');`
- Або через ACF Flexible Content (якщо налаштовано)

---

## 📝 Примітки

- Для роботи AJAX потрібно, щоб скрипт `main.js` був зареєстрований з handle `start-theme-main`
- Локалізація AJAX-параметрів відбувається автоматично через `functions.php`
- Сортування працює за полем `price` (meta_value_num)
- Кількість елементів на сторінку налаштована на 3 (можна змінити в `properties.js` та `properties.php`)

---

## 🔧 Налаштування

### Зміна кількості елементів на сторінку:

1. **JavaScript** (`scripts/components/properties.js`):
   - Змініть `per_page: 3` в AJAX-запиті (рядок ~84)
   - Змініть `per_page: 3` в `defaults` функції `loadProperties` (рядок ~72)

2. **PHP** (`template-parts/sections/properties.php`):
   - Змініть `$properties_per_page = get_query_var('properties_per_page') ?: 3;` (рядок ~11)

---

## 📄 Ліцензія

Проект розроблений для внутрішнього використання.
